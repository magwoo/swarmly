services:
  proxy:
    container_name: swarmly-proxy-dev
    build:
      dockerfile: ./docker/Dockerfile.dev
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./:/app
      - cargo-cache:/usr/local/cargo
      - /var/run/docker.sock:/var/run/docker.sock

  api:
    container_name: swarmly-api-dev
    image: magwoo/helloworld:latest
    labels:
      - proxy.domain=app.ru

volumes:
  cargo-cache:

